{
  "name": "@job-scraper/worker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.2.7",
    "zod": "^3.22.4",
    "@job-scraper/shared": "workspace:*",
    "@job-scraper/cf-ai": "workspace:*"
  }
}
